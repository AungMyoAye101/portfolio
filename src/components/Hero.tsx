import React from "react";
import { Spotlight } from "./ui/Spotlight";

const Hero = () => {
  return (
    <section>
      <div>
        <Spotlight fill="white" />
      </div>
    </section>
  );
};

export default Hero;
